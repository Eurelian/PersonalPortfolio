import React, { Fragment } from "react";
import NavBar from "./navbar";
import Hero from "./Hero";

const Header = () => {
	return (
		<Fragment>
			<div>
				<Hero />
			</div>
		</Fragment>
	);
};

export default Header;
